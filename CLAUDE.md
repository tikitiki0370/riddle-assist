# プロジェクト規約

## レスポンシブ対応
- ブレークポイント (`base`, `sm`, `md` 等) はなるべく使わない
- まず `minChildWidth`, `flexWrap`, `%` 指定など、画面幅に応じてシームレスに適応する方法を検討する
- それで対応できない場合のみ、最終手段としてブレークポイントを使う
- 後方互換性にこだわる必要はない。これは内部コードであり、後方互換性を保つことは技術的負債になる